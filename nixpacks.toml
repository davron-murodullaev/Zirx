[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "fontconfig", "google-fonts"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn main:app --bind 0.0.0.0:$PORT"
